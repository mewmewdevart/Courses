Task 01:
	1. mkdir super_secret_stuff
	2. touch super_secret_stuff/top_secret.txt
	3. nano super_secret_stuff/top_secret.txt
   		********
	4. sudo updatedb
	5. locate top_secret.txt > secret_place.txt

Task 02:
	sudo find / -maxdepth 4 -type f -size +1M -exec ls -lh {} \; | sort -k 5nr > Desktop/filesizes.txt
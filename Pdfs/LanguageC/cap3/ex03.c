#include <stdio.h>

void main ()
{
	/* 1. Por que razao variaveis do tipo float nao devem armazenar valores logicos?
		R: Porquê variaveis do tipo float sao instaveis, elas nao exibem um numero arredondo podendo assim prejudicar o conceito de 0 ser falso e qualquer outro numero mesmo que seja 000000.1 ser verdadeiro
	   2. Indique quais, das seguintes afirmacoes, sao verdadeiras e quais sao falsas.
		R: 2.1 [Verdadeiro] O else de um if é facultativo
		   2.2 [Verdade] Num if sao necessarios parenteses entorno da condição
		   2.3 [Falso] O if pode conter a palavra then opcionalmento
		   2.4 [Verdade] Tanto o componente if como o componente else só podem conter uma unica instrução
		   2.5 [Falso] O if tem que estar em uma linha diferente do else
		   2.6 [Verdade] Na condicao do if pode ser colocada uma constante, uma variavel ou uma expressao.
	   3. Como consegue uma instrucao if-else saber onde termina o if e começa o else, ou se o if tem ou nao else.
		R: Através das instruções data dentro de cada um e a sinalização de um novo if ou else.
	   4. Um bloco pode ser constituido com apenas uma instrução?
		R: Sim!
	   5. Depois de um bloco é obrigatorio o uso de ;?
		R: Nao
	   6. Exercicio de exame!
		//Existe alguma diferença no funcionamento dos seguintes trechos?
		R: Sim, uma operação possui if(x==0) que diz se o resultado de x for igual a zero e outra if(x=0)  esta atribuindo um valor zero ao x
	   7. Exercicio de exame!
		//A identação facilita no processo de:
		R: D)Programação : Ela facilita a leitura do programa!
	   8. Um programa identado é, em geral:
		R: C)Mais legivel que outro que nao seja identado.
	   9. Exercicio de exame!
		//Sempre que o compilador detecta um codigo mal indentado:
		R: D) O compilador nao faz qualquer tipo de verificação na identação
	   10. Indique duas vantagens e duas desvantagens de usar o if-else inves de switch
		R: Vantagem01 : O switch permite apenas instruções direcionadas a exibição de um caractere de um tipo especifico.
		   Vantagem02 : O if-else nos permite trabalhar com numeros, letras e caracteres em suas instruções, sem ser tao depende
	   11. Sera que a instrução break, quando apresentada dentro de um if, passa a execução automaticamente para o else?
		R: Nao!
	   12. Qual é o valor logico que as seguintes expressoes enviam para o if?
		R: a) if(10==5) : Se 10 for igual a 5
		   b) if(()

	*/
}

#include <stdio.h>

void main ()
{
	/* 
	01. Indique quais das seguintes declaracoes estão corretas:
		e)float f,g, c; g)chr ch1='A', ch2='A';
	02.  Uma variavel inteira quando é declarada é sempre iniciada com:
		c) Um numero aleatorio
	03. Indique, na seguinte lista, quais os identificadores corretos e incorretos de variaveis:
		Corretos: a)Valor e)_Kabonga f)MENOS h)a10 j)F1
	04. O nome da variavel..
		a) Deve indicar aquilo que ela armazena d)deve ser o mais explicito possivel f)pode mas nao deve, começar pelo underscore g)pode conter mais do que um caractere
	05. Associe os seguintes tipos correspondentes de leitura e escrita:
		int = %d
		float = %f e %e
		char = %c
		short int = %hd
		long int = %ld
		signed long int = %ld
	06.Indentifique quais das seguintes afirmacoes sao verdadeiras e quais sao falsas:
		a) O tipo float reserva espaço em memoria para um real com precisao simples, enquanto o tipo double reserva espaço para uma variavel com precisao dupla.
		c) O tipo char pode ter os prefixos signed e unsigned
		d) Uma variavel declarada como unsigned pode comportar valores superiores a outra que seja declarada como signed.
		h) O operador modulo (%)nao pode ser utilizado em reais
	07. Identifique os erros de compilação que seriam detectados nos seguintes programas:
		7.1) Declarou as variaveis mas nao as exibiu e nem atribuiu uma função a elas
		7.2) Atribuiu um nome reservado (int) a uma variavel
		7.3) Nao se pode atribuir um valor a varias variaveis logo no inicio
	08. Embora que os programas que se seguem nao tenham erros de compilação, identifique as causas de seu possivel mau funcionamento..
		8.1) O scanf esta sendo utilizado para exibir o texto na tela e coletar entrada do teclado
		8.2) O scanf possui o \n na sua leitura de elementos de entrada do teclado
		8.3) A variavel declarada como inteiro recebe um valor do tipo float pela entrada do teclado (scanf) e é solicitado que seja exibido na tela essa "troca" forçada.
 

	*/

}

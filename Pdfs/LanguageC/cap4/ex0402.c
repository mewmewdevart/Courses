#include <stdio.h>

void main()
{
	//Escreva genericamento o laço do while como um laço while
	/* Classic in menus

	char text;
	do
	{
		printf("");
		scanf("%c", &text);
	}
	while (text='sair' && text='SAIR');




	printf("DIgite uma letra);
	scanf("%c", &letra);
	while (letra='c' && letra='C')
		printf("Clientes");
	while (letra='p' && letra='P')
		printf("Patrocinadores");


	*/

	
}
